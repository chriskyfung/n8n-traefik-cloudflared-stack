# ----------------------------------------------------------------
# n8n Configuration
# ----------------------------------------------------------------

# The host name to access the n8n UI
N8N_HOST=n8n.example.com

# The host name to access the n8n webhooks
N8N_WEBHOOK=webhooks.n8n.example.com

# The timezone to use for n8n
# A list of valid timezones can be found here: https://momentjs.com/timezone/
GENERIC_TIMEZONE=America/New_York

# Whether to run a VACUUM operation on the SQLite database on startup
DB_SQLITE_VACUUM_ON_STARTUP=true

# Specify which nodes not to load. 
# For example, to block nodes that can be a security risk if users aren't trustworthy:
#   NODES_EXCLUDE: ["n8n-nodes-base.executeCommand", "n8n-nodes-base.readWriteFile"].
# To enable all nodes, specify
#   NODES_EXCLUDE: [].
N8N_NODES_EXCLUDE=["n8n-nodes-base.executeCommand", "n8n-nodes-base.localFileTrigger"]

# ----------------------------------------------------------------
# Traefik Configuration
# ----------------------------------------------------------------

# Log level for Traefik (e.g., DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO


# ----------------------------------------------------------------
# Cloudflare Tunnel Configuration
# ----------------------------------------------------------------

# Your Cloudflare Tunnel token
# You can get this from your Cloudflare dashboard
CLOUDFLARE_TUNNEL_TOKEN=your-cloudflare-tunnel-token-here
